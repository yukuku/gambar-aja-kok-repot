name: Build and Release APK

on:
  push:
    branches: [main]
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Release APK
        run: ./gradlew assembleRelease

      - name: Get version
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          else
            # `val appVersionName = "x.y.z"` is declared at the top of
            # app/build.gradle.kts and reused by both `android { versionName }`
            # and the generated BuildInfo, so we extract from there.
            VERSION=$(grep '^val appVersionName' app/build.gradle.kts | sed 's/.*"\(.*\)".*/\1/')
            echo "VERSION=v${VERSION}" >> $GITHUB_OUTPUT
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.VERSION }}
          name: "Release ${{ steps.version.outputs.VERSION }}"
          body: "Gambar Aja Kok Repot - ${{ steps.version.outputs.VERSION }}"
          files: app/build/outputs/apk/release/app-release.apk
          draft: false
          prerelease: false
